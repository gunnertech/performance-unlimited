<%= simple_form_for @completed_survey, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :date %>
  <%= f.input :user_id %>
  <%= f.input :survey_id %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                completed_surveys_path, :class => 'btn' %>
  </div>
<% end %>

<%= simple_form_for @assigned_group, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :group_id %>
  <%= f.input :user_id %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                assigned_groups_path, :class => 'btn' %>
  </div>
<% end %>

<%= simple_form_for @assigned_survey, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :survey_id %>
  <%= f.input :division_id %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                assigned_surveys_path, :class => 'btn' %>
  </div>
<% end %>

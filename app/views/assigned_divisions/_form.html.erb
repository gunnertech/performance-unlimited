<%= simple_form_for @assigned_division, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :division_id %>
  <%= f.input :user_id %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                assigned_divisions_path, :class => 'btn' %>
  </div>
<% end %>

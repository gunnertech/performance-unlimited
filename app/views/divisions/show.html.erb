<%- model_class = Division -%>
<div class="page-header clearfix clearfix row-fluid">
  <h1 class="span8"><%= @division.name %></h1>
  <%= link_to t('history'), current_user, class: 'btn btn-large span2 offset2' %>
</div>

<div class="row-fluid">
  <div class="span6">
    <h3><%=t ('todays_leaderboard') %></h3>
    <table class="table table-striped">
      <thead>
        <tr>
          <th><%=t 'rank' %></th>
          <th><%=t 'name' %></th>
          <th><%=t 'score' %></th>
        </tr>
      </thead>
      <tbody>
        <% @division.users.leaderboard(@division.today,@division.today).each_with_index do |user,i| %>
          <tr>
            <td><%= i+1 %></td>
            <td><%= user %></td>
            <td><%= user.completed_surveys.where{ date == my{@division.today} }.try(:first).try(:score) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="span6">
    <h3><%=t ('alltime_leaderboard') %></h3>
    <table class="table table-striped">
      <thead>
        <tr>
          <th><%=t 'rank' %></th>
          <th><%=t 'name' %></th>
          <th><%=t 'average' %></th>
          <th><%=t 'total' %></th>
        </tr>
      </thead>
      <tbody>
        <% @division.users.active.where{ average != nil }.reorder{ [average.desc,score.desc] }.each_with_index do |user,i| %>
          <tr>
            <td><%= i+1 %></td>
            <td><%= user %></td>
            <td><%= number_with_precision(user.average||0.00,precision: 2) %></td>
            <td><%= user.score %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<% if can?(:update, @division) %>

  <dl class="dl-horizontal">
    <dt><strong><%= model_class.human_attribute_name(:organization_id) %>:</strong></dt>
    <dd><%= link_to @division.organization, @division.organization %></dd>
  
    <dt><strong><%=t 'groups' %>:</strong></dt>
    <dd><%= raw(@division.groups.map{|group| link_to(group, [@division,group])}.join(', ')) %></dd>
  </dl>


  <div class="form-actions">
    <%= link_to t('create_new_group'),
                new_division_group_path(@division), :class => 'btn btn-primary'  %>
              
    <%= link_to t('.back', :default => t("helpers.links.back")),
                divisions_path, :class => 'btn'  %>
    <%= link_to t('.edit', :default => t("helpers.links.edit")),
                edit_division_path(@division), :class => 'btn' %>
    <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                division_path(@division),
                :method => 'delete',
                :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                :class => 'btn btn-danger' %>
  </div>
<% end %>